{
  "$schema": "../schemas/agent-config.schema.json",
  "$comment": "This instance's agent config — uploaded to the per-instance config bucket root at deploy time. Edit values here to customize the instance. The Gemini credential (gemini.api_key) comes from Secret Manager via the GEMINI_API_KEY_SECRET env var and must never be committed here. See the Customizing section of README.md.",
  "mcp": {
    "_comment": "Local-development default only. A deployed instance gets MCP_SERVER_URL from Terraform, pointing at the data-plane service, and the env var wins over this value. Leaving localhost here means a laptop run still finds a local MCP server, and it is never the value that ships.",
    "server_url": "http://127.0.0.1:8082",
    "enabled": true
  },
  "thinking": {
    "mcp_level": "medium",
    "synthesis_level": "medium"
  },
  "template_vars": {
    "_comment": "Substituted into {{instance.*}} placeholders in prompts/. Set these to your own deployment's identity.",
    "name": "Example Data Commons",
    "region": "the world",
    "states_term": "regions",
    "fiscal_year_start": "01-01"
  }
}
