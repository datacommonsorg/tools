{
  "$schema": "./agent-config.schema.json",
  "$comment": "Canonical agent-config.json reference. Copy to agent-config.json, fill in the REPLACE_ME values, and upload to the config bucket root. API keys are NOT configured here \u2014 the agent reads them from Secret Manager (see config/README.md). One instance, one file.",
  "gemini": {
    "api_base": "https://generativelanguage.googleapis.com/v1beta/models",
    "mcp_model": "gemini-3-flash-preview",
    "kb_model": "gemini-3-flash-preview",
    "filestores": [
      "fileSearchStores/REPLACE_ME"
    ],
    "demo_filestores": []
  },
  "mcp": {
    "enabled": true
  },
  "knowledge_base": {
    "enabled": false
  },
  "thinking": {
    "mcp_level": "medium",
    "synthesis_level": "medium"
  },
  "query_param_key": "REPLACE_ME_PER_INSTANCE"
}
